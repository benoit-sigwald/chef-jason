{
  "_comment": "Copie ce fichier en 'mcp.config.json' pour activer les serveurs MCP. Chaque serveur est lancé en stdio. Si le fichier est absent, l'application fonctionne quand même (Claude + web_search), sans enrichissement MCP.",
  "servers": [
    {
      "name": "recipes",
      "description": "Base de recettes interrogeable par nom, ingrédients, type de cuisine ou ID.",
      "command": "npx",
      "args": ["-y", "@degentics-ai/mcp-recipes"],
      "enabled": false
    },
    {
      "name": "pantrist",
      "description": "Inventaire du garde-manger, listes de courses et planification des repas.",
      "command": "npx",
      "args": ["-y", "pantrist-mcp"],
      "enabled": false,
      "env": {
        "PANTRIST_API_KEY": "votre-cle-pantrist"
      }
    },
    {
      "name": "koriander",
      "description": "Cookbook digital : extraction/structuration de recettes depuis une URL ou une photo, valeurs nutritionnelles.",
      "command": "npx",
      "args": ["-y", "koriander"],
      "enabled": false
    }
  ]
}
